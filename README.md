# getkonfi NUR packages

Nix User Repository packages for konfi.

After this repository is registered with NUR, the package will be available as:

```sh
nix shell github:nix-community/NUR#repos.getkonfi.konfi
```

The package expression is generated by GoReleaser from `getkonfi/konfi` releases.
